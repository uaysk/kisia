version: '3.8'

services:
  db:
    image: mariadb:10.5
    container_name: ztna-poc-db-1
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: ztna_db
      MYSQL_USER: ztna_user
      MYSQL_PASSWORD: ztnapassword
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - ztna-net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p$$MYSQL_ROOT_PASSWORD"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - 3307:3306

  dummy-uem:
    build: ./dummy-uem
    container_name: ztna-poc-dummy-uem-1
    ports:
      - "8000:8000"
    networks:
      - ztna-net

  backend:
    build: ./backend
    container_name: ztna-poc-backend-1
    volumes:
      - ./nginx/conf.d:/nginx_conf_d
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      DATABASE_URL: "mysql+pymysql://ztna_user:ztnapassword@db:3306/ztna_db"
      UEM_URL: "http://dummy-uem:8000"
      NGINX_CONTAINER_NAME: "ztna-poc-nginx-1"
      NGINX_CONF_PATH: "/nginx_conf_d"
    depends_on:
      db:
        condition: service_healthy
      dummy-uem:
        condition: service_started
    networks:
      - ztna-net

  nginx:
    build: ./nginx
    container_name: ztna-poc-nginx-1
    ports:
      - "83:80"
    volumes:
      - ./frontend:/usr/share/nginx/html
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/log:/var/log/nginx
    depends_on:
      - backend
    networks:
      - ztna-net

  nginx-exporter:
    build: ./monitoring/nginx-exporter
    container_name: ztna-poc-nginx-exporter-1
    environment:
      LOG_PATH: /var/log/nginx/access.log
      LISTEN_PORT: "9113"
    volumes:
      - ./nginx/log:/var/log/nginx:ro
    depends_on:
      - nginx
    networks:
      - ztna-net

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: ztna-poc-prometheus-1
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --web.enable-lifecycle
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    depends_on:
      - nginx-exporter
    networks:
      - ztna-net

  grafana:
    image: grafana/grafana:11.1.0
    container_name: ztna-poc-grafana-1
    ports:
      - "3001:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin123
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH: /var/lib/grafana/dashboards/nginx-monitoring.json
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards
    depends_on:
      - prometheus
    networks:
      - ztna-net

volumes:
  db_data:
  prometheus_data:
  grafana_data:

networks:
  ztna-net:
    driver: bridge

